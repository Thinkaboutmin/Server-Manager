#! /bin/bash

function check_ip {
	# This should check if there's an IP in the file already
	# and return true if a match is found

	return 0
}

function add_ip {
	# TODO This should add an IP to the file
	# check if there's already one
	
	return 0
}

function file_lexer {
	# TODO This should go through the file
	# and check for each section, such as IP and protocols
	return 0
}

function remove_ip {
	# TODO This should remove an IP from
	# the file
	
	return 0
}

function remove_protocol {
	# TODO This should remove a protocol an IP
	# in the file
	
	return 0
}
